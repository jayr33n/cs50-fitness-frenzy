applicationType: default
defaultPackage: com.jayr33n
testFramework: junit
sourceLanguage: java
buildTool: maven
features: [
  # Dependencies
    data,
    data-jpa,
    graalvm,
    jdbc-hikari,
    postgres,
    testcontainers,
    h2,
    lombok,

    annotation-api,
    app-name,
    http-client,
    java,
    java-application,
    junit,
    logback,
    maven,
    netty-server,
    readme,
    shade,
    yaml
]
